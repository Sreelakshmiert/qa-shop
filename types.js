/**
 * JSDoc typedefs (reference only; not loaded as a script).
 * @typedef {{ sku: string, name: string, category: string, price: number, imageSeed?: string, image?: string }} Product
 * @typedef {{ sku: string, qty: number }} CartLine
 * @typedef {{ items: CartLine[], promos: string[] }} CartState
 */
